# Frontend (Vite) environment variables
# Replace the value with your deployed backend URL when you push to Vercel.
# VITE_API_URL should match the address of the Express server that hosts /api endpoints.
VITE_API_URL="https://your-backend.example.com"

# Backend environment variables
# Copy to .env in the backend folder and fill in your credentials before starting locally.
# MONGO_URI=...
# PORT=5000
# TBO_API_URL=https://api.tbotechnology.in
# TBO_USERNAME=...
# TBO_PASSWORD=...
