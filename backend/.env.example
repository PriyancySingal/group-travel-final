# ========================================
# Backend Environment Variables (Production)
# ========================================

# Server Configuration
NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/group-travel?retryWrites=true&w=majority

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# TBO Hotel API Credentials
TBO_API_URL=https://api.tbotechnology.in/hotelapi_v7/hotelservice.svc/hotels/available
TBO_USERNAME=hackathontest
TBO_PASSWORD=Hackathon@12345

# Frontend URL (update after frontend deployment)
FRONTEND_URL=https://your-frontend-url.vercel.app

# ========================================
# How to get these values:
# ========================================
# 1. MongoDB URI: Get from MongoDB Atlas dashboard
#    - Go to https://cloud.mongodb.com
#    - Click "Connect" on your cluster
#    - Choose "Drivers" → "Node.js"
#    - Copy the connection string
#
# 2. JWT Secret: Generate with:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 3. TBO Credentials: Provided by TBO support
#
# 4. Frontend URL: Will get after deploying frontend to Vercel/Netlify
